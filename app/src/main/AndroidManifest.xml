<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.monika.wisielec_projektzaliczeniowy">

    <application
        android:allowBackup="true"
        android:icon="@drawable/hangman_icon"
        android:label=""
        android:roundIcon="@drawable/hangman_icon"
        android:supportsRtl="true"
        android:theme="@style/RedTheme">
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"
                    />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OnePlayer" />
        <activity android:name=".TwoPlayers" />
        <activity android:name=".CategoryForOnePlayer" />
        <activity android:name=".CategoryForTwoPlayers" />
        <activity android:name=".ScoreClass" />



    </application>

</manifest>